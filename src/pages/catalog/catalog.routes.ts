import { Routes } from '@angular/router';
import { CatalogPageComponent } from './ui/catalog-page.component';

export const catalogRoutes: Routes = [
  { path: '', component: CatalogPageComponent },
];
