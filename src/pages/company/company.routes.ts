import { Routes } from '@angular/router';
import { CompanyPageComponent } from './ui/company-page.component';

export const companyRoutes: Routes = [
  { path: '', component: CompanyPageComponent },
];
