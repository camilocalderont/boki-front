import { Routes } from '@angular/router';
import { DashboardPageComponent } from './ui/dashboard-page.component';

export const dashboardRoutes: Routes = [
  { path: '', component: DashboardPageComponent },
];
